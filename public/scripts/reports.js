(() => {
 const fit = canvas => { const ratio=devicePixelRatio||1,width=canvas.clientWidth||600,height=280;canvas.width=width*ratio;canvas.height=height*ratio;const ctx=canvas.getContext('2d');ctx.scale(ratio,ratio);return {ctx,width,height}; };
 const studentCanvas=document.getElementById('students-chart');if(studentCanvas){const {ctx,width,height}=fit(studentCanvas),data=window.reportStudents,total=data.values.reduce((a,b)=>a+b,0),cx=width/2,cy=height/2,r=88;let start=-Math.PI/2;if(!total){ctx.fillStyle='#e2e8f0';ctx.beginPath();ctx.arc(cx,cy,r,0,Math.PI*2);ctx.arc(cx,cy,48,0,Math.PI*2,true);ctx.fill();}data.values.forEach((value,index)=>{if(!value)return;const end=start+Math.PI*2*value/total;ctx.fillStyle=data.colors[index];ctx.beginPath();ctx.moveTo(cx,cy);ctx.arc(cx,cy,r,start,end);ctx.fill();start=end;});ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(cx,cy,48,0,Math.PI*2);ctx.fill();ctx.fillStyle='#0f172a';ctx.textAlign='center';ctx.font='700 25px sans-serif';ctx.fillText(total,cx,cy+4);ctx.font='12px sans-serif';ctx.fillStyle='#64748b';ctx.fillText('alunos',cx,cy+23);const legend=document.getElementById('students-legend');data.labels.forEach((label,index)=>{legend.insertAdjacentHTML('beforeend',`<span><i style="background:${data.colors[index]}"></i>${label}: ${data.values[index]}</span>`)});}
 const financeCanvas=document.getElementById('finance-chart');if(financeCanvas){const {ctx,width,height}=fit(financeCanvas),data=window.reportFinance,pad={left:58,right:16,top:20,bottom:42},chartW=width-pad.left-pad.right,chartH=height-pad.top-pad.bottom,max=Math.max(1,...data.income,...data.expenses);ctx.strokeStyle='#dbe3ee';ctx.fillStyle='#64748b';ctx.font='11px sans-serif';for(let i=0;i<=4;i++){const y=pad.top+chartH*i/4;ctx.beginPath();ctx.moveTo(pad.left,y);ctx.lineTo(width-pad.right,y);ctx.stroke();ctx.fillText(`R$ ${(max*(1-i/4)).toLocaleString('pt-BR',{maximumFractionDigits:0})}`,2,y+4);}const group=chartW/data.labels.length,bar=Math.min(26,group*.25);data.labels.forEach((label,index)=>{const x=pad.left+group*index+group/2,ih=chartH*data.income[index]/max,eh=chartH*data.expenses[index]/max;ctx.fillStyle='#22c55e';ctx.fillRect(x-bar-2,pad.top+chartH-ih,bar,ih);ctx.fillStyle='#ef4444';ctx.fillRect(x+2,pad.top+chartH-eh,bar,eh);ctx.fillStyle='#64748b';ctx.textAlign='center';ctx.fillText(label,x,height-15);});}
})();
